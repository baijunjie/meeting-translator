{
  "name": "meeting-translator-monorepo",
  "version": "0.1.0-beta.1",
  "description": "Local real-time meeting transcription for macOS (monorepo)",
  "private": true,
  "scripts": {
    "postinstall": "node node_modules/electron/install.js",
    "dev": "pnpm --filter @mt/macos dev",
    "build": "pnpm --filter @mt/macos build",
    "type-check": "pnpm --filter @mt/macos type-check",
    "dist": "pnpm --filter @mt/macos dist",
    "dist:dir": "pnpm --filter @mt/macos dist:dir",
    "set-version": "node scripts/set-version.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "esbuild",
      "onnxruntime-node",
      "sharp"
    ],
    "overrides": {
      "string-width": "^4.2.3"
    }
  }
}
